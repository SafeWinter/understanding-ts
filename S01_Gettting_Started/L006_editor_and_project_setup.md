# L006 Editor & Project Setup

---

（重制版）

推荐 `IDE`：`Visual Studio Code`（https://code.visualstudio.com/）

本套课程无需特定 `VSCode` 扩展，大多数章节都是以空文件夹开始演示 `TS` 的特性。

由于 `VSCode` 内置了对 `TypeScript` 的原生支持，因此甚至都无需单独考虑 `TypeScript` 的编译问题。

个别课程代码可能会涉及项目初始环境的搭建（`npm install`、`npm start` 等）
